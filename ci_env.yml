name: testenv
channels:
  - conda-forge
  - robotology
  - robostack-staging
dependencies:
  # Use the minimum version supported in conda-forge here
  - python==3.9
  - pip
  - pytest
  # packages that ship URDFs used to test
  - icub-models
  - ergocub-software
  - ros-humble-moveit-resources-panda-description
